const User = require('./User');
const Thought = require('./Thought');//not sure if we need these since user references it..
const Reaction = require('./Reaction');//..

module.exports = { User, Thought, Reaction };
